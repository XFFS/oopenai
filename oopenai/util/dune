(executable
 (name ooo_spec_gen)
 (public_name ooosg)
 (libraries yaml
            decoders
            decoders-jsonm
            compiler-libs
            ppxlib
            )
 (preprocess (pps ppxlib.metaquot)))
